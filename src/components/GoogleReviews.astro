<div class="greview-ticker" id="greviewWidget">
  <div class="greview-ticker-tab" onclick="toggleReviews()">
    <div class="greview-ticker-icon">★</div>
    <div class="greview-ticker-score">4.3</div>
    <div class="greview-ticker-chevron" id="tickerChevron">‹</div>
  </div>
  <div class="greview-ticker-panel" id="tickerPanel">
    <div class="greview-panel-inner">
      <div class="greview-panel-header">
        <span class="greview-panel-stars">★★★★★</span>
        <span class="greview-panel-rating">4.3</span>
        <span class="greview-panel-count">(1,200+)</span>
      </div>
      <div class="greview-panel-reviews">
        <div class="greview-panel-review">
          <div class="greview-panel-rstars">★★★★★</div>
          <p>"Great shopping mall with a wide variety of stores. The food court is excellent and the PVR cinema is top-notch."</p>
        </div>
        <div class="greview-panel-review">
          <div class="greview-panel-rstars">★★★★☆</div>
          <p>"Kerala's first lifestyle mall. Well maintained, good parking, and a great place for family outings."</p>
        </div>
        <div class="greview-panel-review">
          <div class="greview-panel-rstars">★★★★★</div>
          <p>"Excellent shopping experience. All major brands available. Clean environment and helpful staff."</p>
        </div>
      </div>
      <a href="https://share.google.com/eoJbb12dGb54a7HQm" target="_blank" class="greview-panel-link">View all on Google →</a>
    </div>
  </div>
</div>

<style>
.greview-ticker {
  position: absolute;
  right: 24px;
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
  display: flex;
  align-items: flex-start;
  gap: 0;
}
.greview-ticker-tab {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: 10px;
  padding: 10px 8px;
  cursor: pointer;
  transition: all 0.3s ease;
  min-width: 48px;
}
.greview-ticker-tab:hover {
  background: rgba(0,0,0,0.75);
  border-color: rgba(255,255,255,0.25);
}
.greview-ticker-icon {
  color: #F4B400;
  font-size: 1.1rem;
  line-height: 1;
}
.greview-ticker-score {
  color: #fff;
  font-size: 1rem;
  font-weight: 700;
  line-height: 1.2;
}
.greview-ticker-chevron {
  color: rgba(255,255,255,0.6);
  font-size: 0.9rem;
  line-height: 1;
  transition: transform 0.3s ease;
}
.greview-ticker.open .greview-ticker-chevron {
  transform: rotate(180deg);
}

/* Panel slides out to the right */
.greview-ticker-panel {
  position: absolute;
  right: calc(100% + 8px);
  top: 50%;
  transform: translateY(-50%);
  width: 340px;
  max-width: calc(100vw - 100px);
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transition: max-height 0.4s ease, opacity 0.3s ease;
  border-radius: 12px;
  background: rgba(0,0,0,0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255,255,255,0.1);
}
.greview-ticker.open .greview-ticker-panel {
  max-height: 460px;
  opacity: 1;
  overflow-y: auto;
}
.greview-panel-inner { padding: 20px; }
.greview-panel-header {
  display: flex;
  align-items: center;
  gap: 8px;
  color: #fff;
  font-size: 0.95rem;
  margin-bottom: 14px;
  padding-bottom: 12px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
}
.greview-panel-stars { color: #F4B400; font-size: 1rem; letter-spacing: 2px; }
.greview-panel-rating { font-weight: 700; font-size: 1.1rem; }
.greview-panel-count { color: rgba(255,255,255,0.6); font-size: 0.85rem; }
.greview-panel-reviews { display: flex; flex-direction: column; gap: 12px; }
.greview-panel-review { line-height: 1.5; }
.greview-panel-rstars { color: #F4B400; font-size: 0.9rem; margin-bottom: 4px; letter-spacing: 2px; }
.greview-panel-review p {
  color: rgba(255,255,255,0.85);
  font-size: 0.88rem;
  margin: 0;
  line-height: 1.5;
}
.greview-panel-link {
  display: block;
  text-align: center;
  margin-top: 14px;
  padding-top: 12px;
  border-top: 1px solid rgba(255,255,255,0.1);
  color: #8AB4F8;
  font-size: 0.88rem;
  font-weight: 500;
  transition: color 0.2s;
}
.greview-panel-link:hover { color: #fff; }

@media (max-width: 768px) {
  .greview-ticker {
    right: 10px;
    top: auto;
    bottom: 20px;
    transform: none;
  }
  .greview-ticker-tab {
    padding: 8px 6px;
    min-width: 40px;
    border-radius: 8px;
  }
  .greview-ticker-icon { font-size: 0.95rem; }
  .greview-ticker-score { font-size: 0.85rem; }
  .greview-ticker-panel {
    right: auto;
    left: 0;
    bottom: calc(100% + 8px);
    top: auto;
    transform: none;
    width: 300px;
  }
  .greview-ticker.open .greview-ticker-panel {
    left: 10px;
  }
}
</style>

<script>
function toggleReviews() {
  const widget = document.getElementById('greviewWidget');
  widget.classList.toggle('open');
}
document.addEventListener('click', function(e) {
  const widget = document.getElementById('greviewWidget');
  if (widget && !widget.contains(e.target)) {
    widget.classList.remove('open');
  }
});
</script>